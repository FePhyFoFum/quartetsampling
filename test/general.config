# required information, values may be changed as necessary but do not change variable names

# args for sampling script
-X = raxmlHPC-PTHREADS-AVX
-e = temp

# other info for test setup
qs_dir = ../pysrc